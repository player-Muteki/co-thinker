# ============================================================
# Co-Thinker — 运行时依赖
# 精确版本已锁定于 requirements.lock
# 修改后运行: pip freeze > requirements.lock
# ============================================================
streamlit>=1.35.0
python-dotenv>=1.0.0
openai>=1.0.0
pytest>=8.0.0
pymupdf>=1.24.0
python-docx>=1.1.0
python-pptx>=1.0.0
